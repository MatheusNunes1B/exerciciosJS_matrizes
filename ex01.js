//Usando `length`

// Crie um array chamado `frutas` com três frutas. Exiba no console o número total de elementos usando `length`.

let frutas = ["maça", "Banana", "Laranja"];
console.log(frutas.length);